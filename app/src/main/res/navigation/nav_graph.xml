<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/searchFragment">

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.sshevtsov.translator.presentation.search.SearchFragment"
        android:label="Переводчик"
        tools:layout="@layout/fragment_search">
        <action
            android:id="@+id/action_searchFragment_to_detailFragment"
            app:destination="@id/detailFragment" />
        <action
            android:id="@+id/action_searchFragment_to_historyFragment"
            app:destination="@id/historyFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.sshevtsov.translator.presentation.detail.DetailFragment"
        android:label="{label}"
        tools:layout="@layout/fragment_detail" >
        <argument
            android:name="dataModel"
            app:argType="com.sshevtsov.translator.domain.model.search.DataModel" />
        <argument
            android:name="label"
            app:argType="string"
            android:defaultValue="Перевод" />
    </fragment>
    <fragment
        android:id="@+id/historyFragment"
        android:name="com.sshevtsov.translator.presentation.history.HistoryFragment"
        android:label="История"
        tools:layout="@layout/fragment_history" />
</navigation>